<tr>
  <td><%= link_to "Transfer", edit_transfer_url(transfer, :host => 'treasury.' + request.domain) %></td>
  <td><%= number_to_currency(transfer.value, :currency => "$") %></td>
  <td><%= transfer.created_at %></td>
  <td></td>
  <td><%= transfer.user.name %></td>
  <td><%= transfer.note %></td>
</tr>