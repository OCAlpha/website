<%= form_for(@charge_type) do |f| %>
  <% if @charge_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@charge_type.errors.count, "error") %> prohibited this charge_type from being saved:</h2>

      <ul>
      <% @charge_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category %><br />
    <%= f.text_field :category %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.label :office %>
    <%= f.select :office_id, Office.all.collect {|o| [o.title, o.id]} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
